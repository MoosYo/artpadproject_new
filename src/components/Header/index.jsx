import classes from "./styles.module.scss";

const {
    header
} = classes;

export default function Header (props) {
    return (
        <header className={header}>
            Header
        </header>
    )
}